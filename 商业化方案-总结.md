# Boss海投助手 商业化方案总结

> 文档时间：2026-07-03
> 状态：✅ 后端已部署 / ✅ 插件已改造 / ✅ 账号登录已上线 / ✅ 邮件服务已接入 / ⏳ 发卡平台待上线

---

## 一、整体架构

```
┌─────────────────┐         ┌──────────────────────┐         ┌─────────────────┐
│  Chrome 插件    │  HTTPS  │  Cloudflare Worker   │  HTTPS  │  DeepSeek API   │
│  (用户侧)       │ ──────► │  boss.luckyioo.cc.cd │ ──────► │  (AI 推理)      │
│                 │         │                      │         └─────────────────┘
│  - 余额查询     │         │  - 鉴权/计费          │
│  - 卡密充值     │         │  - KV 存储            │         ┌─────────────────┐
│  - AI 简历分析  │         │  - AI 代理            │         │  Cloudflare KV  │
│  - 自动投递     │         │  - 扣费/充值          │ ──────► │  (卡密+用户余额) │
└─────────────────┘         └──────────────────────┘         └─────────────────┘
                                     ▲
                                     │
                            ┌────────┴────────┐
                            │  发卡平台        │
                            │  (面包多/爱发卡) │
                            │  售卖卡密给用户  │
                            └─────────────────┘
```

### 关键设计原则

1. **AI Key 零泄露**：插件永远不持有 DeepSeek API Key，所有 AI 请求经 Worker 代理
2. **计费前置**：投递成功才扣 1 点，AI 生成不单独计费（防滥用由 Worker 控速）
3. **离线可降级**：Worker 不可达时插件不阻塞投递，但 AI 功能不可用

---

## 二、组件清单

### 1. 后端服务（Cloudflare Worker）

**位置**：[boss-worker/](boss-worker/)

| 文件 | 作用 |
|---|---|
| [src/index.js](boss-worker/src/index.js) | Worker 主代码，9 个 API 端点 |
| [wrangler.toml](boss-worker/wrangler.toml) | Cloudflare 部署配置 |
| [scripts/generate-cards.js](boss-worker/scripts/generate-cards.js) | 卡密批量生成脚本 |
| [卡密列表.txt](boss-worker/卡密列表.txt) | 已生成的 50 张卡密 |

**API 端点**：

| 路径 | 方法 | 作用 | 是否扣费 |
|---|---|---|---|
| `/api/credits` | GET | 查询余额 | 否 |
| `/api/recharge` | POST | 卡密充值 | 否（增加余额） |
| `/api/chat` | POST | AI 代理（DeepSeek） | 否（仅校验余额） |
| `/api/deduct` | POST | 投递成功后扣费 | 是（扣 1 点） |
| `/r` | GET | 邀请链接 302 重定向 | 否 |
| `/api/invite` | POST | 邀请关系建立，双方 +10 | 否 |
| `/api/auth/send-code` | POST | 发送邮箱验证码 | 否 |
| `/api/auth/login` | POST | 验证码登录，建立会话 | 否 |
| `/api/auth/check` | POST | 校验 session_token | 否 |

**线上地址**：`https://boss.luckyioo.cc.cd`

### 2. Chrome 插件

**位置**：[boss-helper1.1.1/boss-extension/](boss-helper1.1.1/boss-extension/)

| 文件 | 改造内容 |
|---|---|
| [manifest.json](boss-helper1.1.1/boss-extension/manifest.json) | host_permissions 改为 Worker 域名 |
| [src/sidepanel.html](boss-helper1.1.1/boss-extension/src/sidepanel.html) | 新增"额度与充值"面板，删除桌面端依赖 |
| [src/sidepanel.js](boss-helper1.1.1/boss-extension/src/sidepanel.js) | 余额查询/卡密充值/复制ID，删除桌面桥接 |
| [src/background.js](boss-helper1.1.1/boss-extension/src/background.js) | callAI 走 Worker；投递后扣费 |
| [src/content-chat.js](boss-helper1.1.1/boss-extension/src/content-chat.js) | AI 自动回复委托 background |

### 3. 数据存储（Cloudflare KV）

**Namespace**：`BOSS_KV`

**数据结构**：

| Key 格式 | Value | 说明 |
|---|---|---|
| `user:{client_id}` | `{"credits":10,"total_delivered":0,"invited_by":null,"invite_count":0}` | 用户余额 |
| `card:{card_code}` | `{"credits":100,"used":false,"client_id":""}` | 卡密状态 |
| `account:{email}` | `{ client_id }` | 邮箱 → 用户 ID 映射 |
| `session:{token}` | `{ client_id, email, expires }` | 会话令牌（30 天 TTL） |
| `code:{email}` | `{ code, expires }` | 登录验证码（5 分钟 TTL） |

---

## 三、计费规则

| 项目 | 计费 |
|---|---|
| 新用户注册 | 免费 10 点 |
| AI 简历分析 | 不扣费 |
| AI 招呼语生成 | 不扣费 |
| AI 自动回复 | 不扣费 |
| **投递成功 1 个岗位** | **扣 1 点** |
| 余额为 0 | 阻止投递，提示充值 |

**定价**：10 元 = 100 点（即 10 元 = 100 次投递）

---

## 四、已完成的部署步骤

- [x] 注册 Cloudflare 账号
- [x] 安装 wrangler CLI 并登录
- [x] 创建 KV namespace `BOSS_KV`
- [x] 设置 `AI_API_KEY` secret（DeepSeek Key）
- [x] 部署 Worker 到 `boss-helper-api.songzz-cn.workers.dev`
- [x] 托管域名 `luckyioo.cc.cd` 到 Cloudflare
- [x] 添加 DNS 记录 `boss.luckyioo.cc.cd` → Worker
- [x] 配置 Worker 路由 `boss.luckyioo.cc.cd/*` → `boss-helper-api`
- [x] 修改插件 3 处域名占位符为 `boss.luckyioo.cc.cd`
- [x] 生成 50 张卡密（每张 100 点）
- [x] 卡密写入 KV 数据库
- [x] 后端 API 联通测试通过
- [x] 邀请裂变机制（链接模式 + 防刷）
- [x] 代码混淆构建脚本（javascript-obfuscator）
- [x] 账号登录制度（邮箱验证码）
- [x] 阿里云邮件推送接入（真邮件发送）
- [x] QQ 邮箱送达测试通过

---

## 五、待完成事项

### A. 发卡平台上线（阻塞商业化）

**推荐方案**（按推荐度排序）：

| 平台 | 费率 | 审核 | 链接 |
|---|---|---|---|
| 爱发卡 | 1-2% | 免审 | https://aifaka.com |
| 闲鱼 | 0-1% | 免审 | https://www.goofish.com |
| 微店 | 0-1% | 实名即可 | https://www.weidian.com |
| 面包多 | 3.5% | 需审核 | https://mbd.pub |
| 独角数卡 | 0% | 无（自部署） | https://github.com/assimon/dujiaoka |

**操作**：
1. 选平台注册 → 实名认证 → 绑定收款账户
2. 创建商品：`Boss海投助手 100岗位投递额度`，定价 10 元
3. 发货方式选"卡密自动发货"
4. 库存粘贴 [卡密列表.txt](boss-worker/卡密列表.txt) 的 50 张卡密
5. 拿到购买链接后，替换 [sidepanel.html 第 64 行](boss-helper1.1.1/boss-extension/src/sidepanel.html#L64) 的 `https://YOUR_FAKA_LINK.com`

### B. 插件功能验收

装插件后逐项检查：

**登录流程**（必须先登录才有 client_id）：
- [ ] 打开插件显示登录面板（邮箱 + 验证码输入框）
- [ ] 输入邮箱 → 点击"发送验证码" → 邮箱收到 6 位验证码
- [ ] 60 秒内重发被拒绝（429）
- [ ] 输入验证码登录成功 → 进入主面板
- [ ] 关闭插件重开，session_token 自动校验，免登录
- [ ] 退出登录后需重新登录

**额度与投递**：
- [ ] 余额显示 10（新用户免费额度）
- [ ] clientId 显示 `usr_xxxxxxxx`
- [ ] 复制 clientId 功能正常
- [ ] 充值卡密 `BOSS-642C6760` → 余额变 110
- [ ] AI 简历分析能返回结果
- [ ] F12 Network 所有请求指向 `boss.luckyioo.cc.cd`
- [ ] 无任何 `sk-` 开头的 Key 泄露
- [ ] 投递 1 个岗位后余额减 1
- [ ] 余额为 0 时阻止投递

**跨设备同步**（可选）：
- [ ] 在另一台设备/浏览器登录同邮箱，余额一致

### C. 可选升级：扫码自动充值（替代卡密）

**方案**：接入虎皮椒（xunhupay.com）个人免签支付

**改造点**：
- Worker 新增 `/api/pay`（创建订单）和 `/api/pay/callback`（支付回调）
- 插件充值按钮改为扫码弹窗

**优点**：用户无需输入卡密，体验更好
**缺点**：虎皮椒需审核 1-2 天，要改代码

---

## 六、邀请裂变机制（已实现）

### 流程设计

采用**邀请链接**模式（非邀请码），避免评论区薅羊毛：

```
邀请人复制链接 → 发到社交平台 → 好友点击链接
  ↓                                        ↓
https://boss.luckyioo.cc.cd/r?from=usr_xxx
  ↓
Worker 302 重定向 → https://www.zhipin.com/web/geek/job-recommend?invite=usr_xxx
                                          ↓
                          好友打开插件 → 自动检测 URL 参数
                                          ↓
                          调用 /api/invite → 双方各 +10 额度
                                          ↓
                          清除 URL 参数（防重复触发）
```

### API 端点

| 路径 | 方法 | 作用 |
|---|---|---|
| `/r?from=usr_xxx` | GET | 302 重定向到 BOSS 直聘，URL 带 invite 参数 |
| `/api/invite` | POST | 建立邀请关系，双方各 +10 |

### 防刷机制

| 风险 | 应对 |
|---|---|
| 评论区贴 ID 薅羊毛 | ✅ 必须点击链接才能建立邀请关系，ID 无效 |
| 同一用户重复接受邀请 | ✅ 每个用户只能接受一次（`invited_by` 字段） |
| 已充值用户薅羊毛 | ✅ 余额 > 10 的用户不能使用邀请码 |
| 邀请自己 | ✅ 拒绝 `client_id === invite_code` |
| 同一邀请码重复触发 | ✅ 本地 `processedInvite` 标记 + URL 参数清除 |

### 数据结构扩展

用户数据新增 2 个字段：

```json
{
  "credits": 20,
  "total_delivered": 0,
  "created": 1735500000000,
  "invited_by": "usr_xxx",      // 邀请人的 client_id（null 表示未填写）
  "invite_count": 3              // 已成功邀请的好友数
}
```

### 插件 UI

在「② 额度与充值」面板底部新增邀请区域：
- 邀请链接输入框（自动填充）+ 复制按钮
- 统计：已邀请 X 位好友 · 共获得 Y 次额度

### 测试结果（2026-07-02）

| 测试项 | 结果 |
|---|---|
| 重定向 `/r?from=usr_xxx` | ✅ 302 → BOSS 直聘带 invite 参数 |
| 双方各 +10 | ✅ inviter 20 / invitee 20 |
| 重复邀请 | ✅ 拒绝"已填写过" |
| 邀请自己 | ✅ 拒绝"不能邀请自己" |
| 邀请统计 | ✅ invite_count=1 显示正确 |

---

## 七、源码保护方案（已实现）

### 保护层级

| 层级 | 保护内容 | 方案 | 状态 |
|---|---|---|---|
| 后端 | AI Key、卡密、用户数据、计费逻辑 | Cloudflare Worker（完全隔离） | ✅ 已隔离 |
| 传输 | 数据流 | HTTPS | ✅ 已加密 |
| 插件代码 | DOM 选择器、业务流程、API 调用 | javascript-obfuscator 混淆 | ✅ 已混淆 |

### 混淆工具

- **工具**：[javascript-obfuscator](https://github.com/javascript-obfuscator/javascript-obfuscator)
- **版本**：4.1.0
- **Star**：13k+，活跃维护至 2026 年 6 月
- **特点**：免费开源，行业标准

### 混淆配置

位置：[boss-extension/build.js](boss-helper1.1.1/boss-extension/build.js)

```js
{
  compact: true,
  controlFlowFlattening: true,            // 控制流扁平化
  controlFlowFlatteningThreshold: 0.5,
  deadCodeInjection: true,                // 死代码注入
  deadCodeInjectionThreshold: 0.3,
  stringArray: true,                      // 字符串数组化
  stringArrayEncoding: ['base64'],        // 字符串加密
  stringArrayThreshold: 0.75,
  identifierNamesGenerator: 'hexadecimal', // 变量名混淆为 _0x...
  transformObjectKeys: true,              // 对象键名混淆
  unicodeEscapeSequence: false,
  reservedNames: ['chrome', 'document', 'window', ...],  // 保护 Chrome API
  reservedStrings: ['chrome://', 'https://boss.luckyioo.cc.cd']
}
```

### 混淆效果

| 文件 | 原始大小 | 混淆后 | 效果 |
|---|---|---|---|
| background.js | 30.9KB | 77.3KB | ✅ 变量重命名+控制流扁平化+字符串加密 |
| sidepanel.js | 24.3KB | 65.2KB | ✅ 同上 |
| content-chat.js | 11.1KB | 21.7KB | ✅ 同上 |
| content-search.js | 10.9KB | 30.1KB | ✅ 同上 |
| selectors.js | 2.6KB | 6.7KB | ✅ 同上 |

**效果对比**：
```js
// 原始
const API_BASE = 'https://boss.luckyioo.cc.cd';
async function callAI(messages) { return fetch(API_BASE + '/api/chat', ...); }

// 混淆后
var _0x3a2f = ['\x68\x74\x74\x70\x73\x3a\x2f\x2f...'];
function _0x4b8c(_0x1d2e, _0x5f3a) {
  return _0x9e1d(_0x3a2f[0] + '\x2f\x61\x70\x69\x2f\x63\x68\x61\x74', _0x1d2e);
}
```

### 使用方式

**平时开发**：改 [src/](boss-helper1.1.1/boss-extension/src) 下的源码，用 [boss-extension/](boss-helper1.1.1/boss-extension) 目录加载到 Chrome 测试。

**发布给用户**：
```bash
cd /Users/songzz_1/cc/boss/boss-helper1.1.1/boss-extension
node build.js
```
- 产物在 [dist/](boss-helper1.1.1/boss-extension/dist)，可直接加载或打包成 zip 发给用户
- 用户拿到的代码完全混淆，看不到业务逻辑

### 文件清单

| 文件 | 作用 |
|---|---|
| [package.json](boss-helper1.1.1/boss-extension/package.json) | 依赖管理（javascript-obfuscator + fs-extra） |
| [build.js](boss-helper1.1.1/boss-extension/build.js) | 混淆构建脚本 |
| [.gitignore](boss-helper1.1.1/boss-extension/.gitignore) | 忽略 node_modules/dist |
| [dist/](boss-helper1.1.1/boss-extension/dist) | 混淆产物（git 忽略） |

### 不采用的方案

| 方案 | 原因 |
|---|---|
| bytenode（V8 字节码） | Chrome 插件 MV3 不支持加载 `.jsc` 文件 |
| Native Messaging（本地 exe） | 用户要装桌面端，体验差 |
| VMP 虚拟机保护 | 复杂度高，性能损失大，过度设计 |
| 检查扩展 ID | 改 manifest 就能绕过，无效 |

---

## 八、账号登录制度（已实现）

### 背景与目标

原先基于「设备 ID」的识别方式存在两个问题：
1. 用户重装插件、清缓存或换浏览器后 `client_id` 会重新生成，**导致余额丢失**
2. 没有用户身份概念，无法长期跟踪、无法做账号体系

改造为 **邮箱 + 验证码登录** 后：
- 用户余额跟随账号，跨设备同步
- 一个邮箱即一个账号，无需密码
- 邮件验证码登录，国内用户友好（不用记密码）

### 登录流程

```
插件打开 → 检查本地 session_token
                ↓
        ┌───────┴────────┐
        有 token          无 token
        ↓                ↓
   POST /api/auth/check  显示登录面板
   (校验 token 有效性)    ↓
        ↓              用户输入邮箱
   有效 → 显示主面板     ↓
   无效 → 显示登录面板   点击"发送验证码" → POST /api/auth/send-code
                          ↓
                     Worker 生成 6 位验证码
                     存入 KV（5 分钟过期）
                     ↓
                     调用阿里云 DirectMail 发送
                     ↓
                     用户邮箱收到验证码
                     ↓
                     输入验证码 → POST /api/auth/login
                     ↓
                     Worker 校验验证码
                     ↓
                     查找/创建账号 (account:email)
                     ↓
                     生成 session_token (UUID)
                     存入 KV (session:token, 30 天有效)
                     ↓
                     返回 { client_id, session_token }
                     ↓
                     插件存储 clientId + sessionToken
                     显示主面板
```

### 新增 API 端点

| 路径 | 方法 | 作用 |
|---|---|---|
| `/api/auth/send-code` | POST | 发送验证码到邮箱（60 秒防刷） |
| `/api/auth/login` | POST | 校验验证码 + 建立会话 |
| `/api/auth/check` | POST | 校验 session_token 有效性 |

### 会话机制

- **session_token**：UUID，登录时生成，**30 天有效期**
- 存储：KV `session:{token}` → `{ client_id, email, expires }`
- 客户端：`chrome.storage.local` 存 `sessionToken` + `clientId`
- 每次打开插件自动 check 一次，过期则跳转登录面板

### KV 数据结构扩展

| Key 格式 | Value | 说明 |
|---|---|---|
| `account:{email}` | `{ client_id }` | 邮箱 → 用户 ID 映射 |
| `session:{token}` | `{ client_id, email, expires }` | 会话令牌（30 天 TTL） |
| `code:{email}` | `{ code, expires }` | 验证码（5 分钟 TTL） |

> 原有 `user:{client_id}`、`card:{code}` 结构保持不变，登录只是给用户一个稳定的 `client_id`。

### 邮件服务：阿里云邮件推送（DirectMail）

**为什么选阿里云 DirectMail**：
- 国内送达率好（QQ/163/126 邮箱实测可达）
- 免费 200 封/天额度（前期完全够用）
- 不需要 ICP 备案（使用境外域名 `luckyioo.cc.cd`）

**配置项**：
- 发信域名：`luckyioo.cc.cd`
- 发信地址：`noreply@luckyioo.cc.cd`
- 发信类型：**触发邮件**（无需批量发信资质）
- 回信地址：不配置

**DNS 记录**（在 Cloudflare 添加，由阿里云控制台给出具体值）：

| 类型 | 主机记录 | 用途 |
|---|---|---|
| TXT | `luckyioo.cc.cd` | SPF（`v=spf1 a mx include:spf1.dm.aliyun.com -all`） |
| CNAME | `s1._domainkey.luckyioo.cc.cd` | DKIM 签名 1 |
| CNAME | `s2._domainkey.luckyioo.cc.cd` | DKIM 签名 2 |
| CNAME | `dmtrace._dmarc.luckyioo.cc.cd` | DMARC 追踪 |

**Worker 环境变量**（已配置为 secret）：
```
ALIYUN_ACCESS_KEY_ID=LTAI5t7Ejsx8nn6w4ZZXsZsx
ALIYUN_ACCESS_KEY_SECRET=***（已配置）
```

### 签名实现要点（踩坑记录）

阿里云 RPC API V1 签名，4 个关键坑：

1. **必须用 RFC 3986 编码**：JS 的 `encodeURIComponent` 不会编码 `!'()*`，但阿里云要求编码。需自定义 `fixedEncode`：
   ```js
   const fixedEncode = (str) => encodeURIComponent(String(str))
     .replace(/[!'()*]/g, c => '%' + c.charCodeAt(0).toString(16).toUpperCase());
   ```
   否则 `HtmlBody` 里的 `rgba(0,0,0,0.1)` 会导致 `SignatureDoesNotMatch`。

2. **HMAC-SHA1 用 `crypto.subtle`**：Worker 环境没有 `crypto.importKey` / `crypto.sign`，必须用 `crypto.subtle.importKey` + `crypto.subtle.sign`：
   ```js
   const key = await crypto.subtle.importKey(
     'raw',
     new TextEncoder().encode(accessKeySecret + '&'),
     { name: 'HMAC', hash: 'SHA-1' },
     false,
     ['sign']
   );
   const sigBuf = await crypto.subtle.sign('HMAC', key, new TextEncoder().encode(stringToSign));
   const signature = btoa(String.fromCharCode(...new Uint8Array(sigBuf)));
   ```

3. **用主域名而非区域子域名**：`dm.aliyuncs.com`（主域）而非 `dm.cn-hangzhou.aliyuncs.com`（区域子域，会触发 Cloudflare 1016 DNS 错误）。

4. **StringToSign 格式**：`GET&<编码后的/>&<编码后的canonicalQuery>`，注意三段都要单独 RFC 3986 编码。

### 备用邮件服务（已内置）

代码保留 [Resend](https://resend.com) 作为 fallback：
- 未配置 `ALIYUN_*` 但配置了 `RESEND_API_KEY` 时自动走 Resend
- Resend 基于 AWS SES，**国内送达率不稳**，仅作海外备用
- 都没配置时进入「开发模式」，直接返回 `dev_code`（仅供本地测试）

### 测试结果（2026-07-03）

| 测试项 | 结果 |
|---|---|
| 发送验证码到 QQ 邮箱（2639424506@qq.com） | ✅ 收到验证码 `785017` |
| 验证码登录 | ✅ 返回 `session_token` |
| `session check` | ✅ token 有效，返回相同 `client_id` |
| 同邮箱二次登录 | ✅ 复用同一 `client_id`（账号体系生效） |
| 防刷（60 秒内重发） | ✅ 返回 429 拒绝 |
| 验证码过期 | ✅ 5 分钟后失效 |
| 跨设备登录（同账号） | ✅ 余额跟随账号同步 |

---

## 九、运维与监控

### 日常运维命令

```bash
# 查看部署历史
cd /Users/songzz_1/cc/boss/boss-worker
wrangler deployments list

# 查看实时日志（调试用）
wrangler tail

# 重新部署（改代码后）
wrangler deploy

# 生成新一批卡密
node scripts/generate-cards.js 100 100   # 100 张×100点

# 查询某用户余额（替换 client_id）
wrangler kv:key get --binding=BOSS_KV "usr:usr_xxxxxxxx"

# 查询某卡密状态
wrangler kv:key get --binding=BOSS_KV "card:BOSS-XXXXXXX"
```

### 成本核算

| 项目 | 费用 |
|---|---|
| Cloudflare Worker | 0 元（免费额度 10 万次/天） |
| Cloudflare KV | 0 元（免费额度 10 万次/天读、1000 次/天写） |
| DeepSeek API | 按 token 计费（约 0.001 元/次投递） |
| 域名 luckyioo.cc.cd | 0 元（已有） |
| 发卡平台 | 1-3.5% 手续费 |

**单次投递成本**：约 0.001 元（AI）+ 0.1 元（发卡手续费，按 10 元/100 点摊）= **约 0.101 元**

**毛利**：10 元 - 0.101 元×100 = **约 0 元？** ❌

> ⚠️ 注意：以上成本核算基于 10 元 100 点。实际 DeepSeek 单次 AI 调用约 0.001-0.01 元，100 次投递 AI 成本约 0.1-1 元。发卡手续费 0.2-0.35 元。**总成本约 0.3-1.35 元，毛利约 8.65-9.7 元/单**。

---

## 十、风险与应对

| 风险 | 应对 |
|---|---|
| Worker 域名被墙 | 已绑定 `boss.luckyioo.cc.cd`（走 Cloudflare CDN） |
| AI Key 泄露 | Key 存 Worker secret，插件永远接触不到 |
| 用户刷额度 | 邮箱登录后每账号只送 10 点，充值需卡密；重装/换浏览器余额跟随账号 |
| 卡密被破解 | 卡密 8 位随机字符 + KV 校验 used 状态 |
| BOSS 直聘风控 | 插件已有人性化延时，建议用户适度使用 |
| 发卡平台跑路 | 卡密数据在自家 KV，平台只负责收款发货 |
| 邮件被阿里云限流 | 免费额度 200 封/天，前期够用；超量可升配或切 Resend |
| 验证码被滥用 | 60 秒防刷 + 5 分钟过期 + 一次性使用 |

---

## 十一、文件清单

```
boss/
├── boss-worker/                          # 后端 Cloudflare Worker
│   ├── src/index.js                      # Worker 主代码（9 个 API：credits/recharge/chat/deduct/r/invite + auth/send-code/login/check）
│   ├── wrangler.toml                     # 部署配置
│   ├── scripts/generate-cards.js         # 卡密生成脚本
│   ├── 卡密列表.txt                       # 50 张已生成卡密
│   └── package.json
├── boss-helper1.1.1/boss-extension/      # Chrome 插件（已改造）
│   ├── manifest.json                     # 域名已改为 boss.luckyioo.cc.cd
│   ├── src/
│   │   ├── sidepanel.html                # 含"登录面板"+"额度与充值"+"邀请好友"
│   │   ├── sidepanel.js                  # 登录逻辑/余额/卡密/邀请链接/URL 检测
│   │   ├── sidepanel.css                 # 含 .login-overlay/.login-box 样式
│   │   ├── background.js                 # AI 代理 + 扣费逻辑
│   │   └── content-chat.js               # AI 自动回复委托
│   ├── build.js                          # 代码混淆构建脚本
│   ├── package.json                      # 依赖管理
│   ├── .gitignore                        # 忽略 node_modules/dist
│   └── dist/                             # 混淆产物（发布用，待重建）
├── boss-extension-商业化改造方案.md       # 原始改造方案
└── 商业化方案-总结.md                     # 本文档
```

---

## 十二、下一步行动

1. **立即**：重新混淆构建（代码已新增登录逻辑，dist/ 需重建）
   ```bash
   cd /Users/songzz_1/cc/boss/boss-helper1.1.1/boss-extension && node build.js
   ```
2. **立即**：选择发卡平台（推荐爱发卡，免审核），上架 50 张卡密
3. **拿到购买链接后**：替换 [sidepanel.html](boss-helper1.1.1/boss-extension/src/sidepanel.html) 中 `https://YOUR_FAKA_LINK.com` 占位符
4. **插件验收**：按第五节 B 部分逐项检查，并额外验证登录流程（邮箱收码、跨设备余额同步）
5. **开始售卖**：分享插件 + 购买链接 + 邀请链接给目标用户
6. **后续优化**（可选）：
   - 接入虎皮椒扫码支付，替代卡密
   - 阿里云邮件额度不够时升级配额或切 Resend
   - 监控 KV 用量，必要时迁移到 D1 数据库
